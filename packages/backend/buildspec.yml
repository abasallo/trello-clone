version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  paths:
    - './build/**/*'
    - './package.json'

cache:
  paths:
    - './node_modules/**/*'
