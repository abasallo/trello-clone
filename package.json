{
  "name": "aws-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "version": "0.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  }
}
